import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  leftButton: {
    flex: 1,
    width: 5,
    height: 5,
    resizeMode: 'contain',
  },
  header: {
    width: 200,
  },
  input: {
    marginTop: 20,
  },
});
